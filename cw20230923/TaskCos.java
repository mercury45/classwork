public class TaskCos {
	public static void main(String[] args) {
		double s = 0;
		int k = 1
		int sign = 1;
		double x = Math.PI / 2
		double deg = ;
		while (Math.abs(a) > EPS) {
			s += a;
			k += 1;
			sign = -1 
			a = 
		}
	}
}